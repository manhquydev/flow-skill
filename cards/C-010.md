# C-010 — v0.5 release: version coherence (all 0.5.0) + quality-metrics record

status: done
deps: C-006, C-007, C-008, C-009

## Scope

Cut v0.5: bump every declared version to 0.5.0 (SKILL.md frontmatter, portable-manifest.json,
.claude-plugin/plugin.json — the last was stuck at 0.3.0, caught by the now-fixed coherence check)
and bump the README status + quality-metrics version, and record the v0.5 hardening run +
corrected test counts (249 dev / 271 grand) in docs/quality-metrics.md. Doc/manifest layer only —
the engine edits (flow.sh coherence, harness --note alias, run-state gitignore) are the separate
out-of-band maintenance step (`/flow` forbids runner edits as a card).

## Allowed files

- `skills/flow/SKILL.md` · `portable-manifest.json` · `.claude-plugin/plugin.json`
- `README.md` · `README_VN.md` · `docs/quality-metrics.md`

## Verify (run these before calling the card done)

- [x] All declared versions = 0.5.0 (SKILL.md, portable-manifest.json, plugin.json)
- [x] `flow.sh coherence` → PASS (declared versions agree) — proof of the DF-1 fix working
- [x] quality-metrics test table = 249 dev / 271 grand; v0.5 hardening section + DF status present
- [x] READMEs say v0.5
- [x] No edits outside Allowed files

## Done-evidence (world-state proof, named BEFORE building)

`flow.sh coherence` prints "PASS: declared versions agree" listing plugin.json/manifest/SKILL.md all
0.5.0 (the anti-drift tool, formerly blind to skill-type versions, now validates the release);
quality-metrics carries the corrected counts + v0.5 record.

## Evidence (paste the actual proof here when done)

```
$ bash skills/flow/runner/flow.sh coherence | tail -5
  0.5.0  .claude-plugin/plugin.json
  0.5.0  portable-manifest.json
  0.5.0  skills/flow/SKILL.md
  PASS: declared versions agree.
$ grep -E 'Total .dev.|Grand total' docs/quality-metrics.md   # 249 ; 271
```
World-state: v0.5 ships coherent across all version fields — verified by the very coherence command
this run fixed (DF-1); the metrics record reflects the review scorecard + the fixes applied.