# C-002 — /flow constitution advisory command

status: done
deps: none
implements: FR1

## Scope

Add a standalone advisory command `flow.sh constitution` that validates an operator-authored
`flow/constitution.md` of project invariants: structure check (no leftover placeholder, each
invariant has a stable ID + applies-at stage + optional grep-marker), advisory warnings for unmet
markers, graceful skip if no file. ONE thing: the advisory command + its template + its semantic
gate section. **Must NOT be called from `cmd_next`** (no hot-path coupling). Repo root:
`D:\project\flow\flow-skill`.

## Allowed files

- `skills/flow/runner/flow.sh` (new `cmd_constitution` + dispatch case + usage; recall surfacing)
- `skills/flow/_templates/constitution.md` (new template)
- `skills/flow/references/gate-rules.md` (new "Constitution challenge" semantic section)
- `skills/flow/references/command-dispatch.md`, `skills/flow/SKILL.md` (command table + reference list)
- `skills/flow/tests/test_flow_constitution.sh` (new), `skills/flow/tests/run_all.sh` (register)

## Verify (run these before calling the card done)

- [x] Clean `constitution.md` → exit 0; leftover-placeholder or missing-ID → exit 1; unmet marker → exit 0 + warning; no file → graceful skip. (suite A–E)
- [x] `grep` of `flow.sh` proves `cmd_constitution` is **never invoked by `cmd_next`** (0 call sites). (suite H)
- [x] `flow recall` surfaces the active constitution. (suite I)
- [x] New `test_flow_constitution.sh` green (21/21); full `run_all.sh` green, 0 regressions.
- [x] Pure bash + awk/sed/grep (Git-Bash-compatible); the `.cmd` launcher is an unchanged thin dispatcher → portable by construction.

## Done-evidence (world-state proof, named BEFORE building)

Pasted terminal output showing: (a) `flow.sh constitution` producing each of the 4 outcomes
(clean/placeholder-fail/missing-ID-fail/unmet-warn) on fixtures, (b) the grep proving no `cmd_next`
coupling, (c) `run_all.sh` all-green summary. World-state = the installed command behaving + green rig.

## Evidence (paste the actual proof here when done)

Real command behavior (the new `flow.sh constitution` executed for real on fixtures):

```
$ bash tests/test_flow_constitution.sh
A) no file -> graceful skip, exit 0
B) clean filled table -> PASS, exit 0
C) leftover placeholder -> FAIL, exit 1
D) row with no ID -> FAIL, exit 1
E) grep-marker present (ok) / absent (advisory warn, exit 0)
F) malformed 2-cell row -> FAIL, exit 1
G) pipe-rows inside a code fence are ignored -> exit 0
H) LAW: cmd_next never calls cmd_constitution (verified non-empty body)
I) recall surfaces the project constitution
RESULT: 21 passed, 0 failed

$ bash tests/run_all.sh   ->  ALL SUITES PASSED  (18 suites, 0 regressions)
```

Cross-model review (Codex/GPT-5.x) found 3 real issues the 18-suite pass missed:
(1) row parser scanned fenced code blocks; (2) a 2-cell malformed row passed as valid;
(3) the cmd_next test's sed range could silently go empty on refactor. ALL FIXED + locked with
suite cases F/G/H. Advisory-only, never wired into `cmd_next` (verified by test H).
