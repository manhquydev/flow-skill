# C-007 — P1 consistency: contract I1 two-state + drop false review-parenthetical + refresh stale table

status: done
deps: C-006

## Scope

Three P1 consistency fixes from the review: (1) **Contract I1 drift** — `flow/05-contract.md`
still defines detection as ELIGIBLE=INSTALLED; update I1 row + the "Detection rule (I1)" block to
the USABLE two-state (probe required). (2) **False capability claim** — `adversarial-review.md`
implies `codex:codex-rescue` can run reviews, but that subagent only forwards to `task`; drop the
parenthetical, route review via `codex-companion.mjs`. (3) **Self-stale record** — `docs/quality-metrics.md`
test-coverage table says 93 dev / 6 suites; reality is 243 dev / 15 suites — refresh it.

## Allowed files

- `flow/05-contract.md`
- `skills/flow/references/adversarial-review.md`
- `docs/quality-metrics.md`

## Verify (run these before calling the card done)

- [x] `flow/05-contract.md` I1 names USABLE + required setup probe (usable count 3)
- [x] `adversarial-review.md` no longer claims `codex:codex-rescue` runs reviews (0); states "only forwards to task" (1)
- [x] `quality-metrics.md` table reflects 243 dev / 15 suites (grand total 265)
- [x] No edits outside Allowed files (05-contract, adversarial-review, quality-metrics this card)

## Done-evidence (world-state proof, named BEFORE building)

`grep` shows USABLE in the contract I1, the removed review-parenthetical, and the corrected
243/15 counts in quality-metrics.md; `git diff --name-only` = only the 3 allowed files.

## Evidence (paste the actual proof here when done)

```
$ grep -ci usable flow/05-contract.md                       # 3  (I1 row + detection rule = USABLE two-state)
$ grep -c 'codex:codex-rescue.*review' adversarial-review.md # 0  (false claim removed)
$ grep -c 'only forwards to .task' adversarial-review.md     # 1  (correct: review via codex-companion)
$ grep table quality-metrics.md                              # Total(dev) 243, 15 suites, Grand total 265
```
World-state: the contract (the do-not-improvise seam) now carries the USABLE two-state, matching
every operational doc — DIM1-F1 drift closed at its source; the false "rescue can review" claim is
gone; the coherence record (quality-metrics test table) is no longer self-stale.
