# C-016 — v0.12 release: version coherence + agent-wiring tripwire

status: done
deps: C-011, C-012, C-013, C-014, C-015
group: release
implements: W7

## Scope

Cut v0.12.0. Two things:

1. **Version coherence.** Bump every declared version to `0.12.0` (`skills/flow/SKILL.md` frontmatter,
   `portable-manifest.json`, `.claude-plugin/plugin.json`) and the README status lines; add the
   v0.12 CHANGELOG entry (telemetry-truth FR1-FR2 + orchestration-depth FR3-FR4 + engine-hardening
   FR5-FR6 + the wiring tripwire). `/flow coherence` must PASS or the release is blocked.
2. **Agent-wiring tripwire (FR-7).** Add a consistency test asserting every agent named in
   `references/agent-detection.md` is wired into at least one stage in `agent-stage-mapping.md` OR an
   explicitly-labelled repair/review/diagnostic seam — so a future "declared but unwired" gap (the
   exact `debugger` defect this release fixes) trips CI instead of shipping silently.

Doc/manifest/test layer only — no engine logic changes (those shipped in C-011/012/015). Mirrors the
C-010 release pattern.

## Allowed files

- `skills/flow/SKILL.md` · `portable-manifest.json` · `.claude-plugin/plugin.json`
- `README.md` · `README_VN.md` · `CHANGELOG.md` · `docs/quality-metrics.md`
- `tests/test_flow_coverage_gaps.sh` (or a new tiny suite) for the agent-wiring tripwire + `tests/run_all.sh` if a new suite file is added

## Verify (run before calling the card done)

- [x] All declared versions = `0.12.0` (SKILL.md, portable-manifest.json, plugin.json)
- [x] `flow.sh coherence` → PASS (declared versions agree)
- [x] CHANGELOG has a `0.12.0` entry covering every shipped fix (C-011..C-015) + tripwire
- [x] READMEs state v0.12 (badge: 21 suites / 458 checks)
- [x] Agent-wiring tripwire test is green AND actually fails when an agent is removed from a stage (negative-control proven)
- [x] `docs/quality-metrics.md` updated with the v0.12 run + new test counts (21 suites / 458 checks)
- [x] No edits outside Allowed files
- [x] `bash tests/run_all.sh` → ALL SUITES PASSED

## Done-evidence (world-state proof, named BEFORE building)

`flow.sh coherence` prints PASS listing plugin.json/manifest/SKILL.md all at 0.12.0; the new
agent-wiring tripwire is green and demonstrably red when `debugger` is unwired (negative control);
`bash tests/run_all.sh` ends ALL SUITES PASSED with the updated count recorded in quality-metrics.

## Evidence (paste actual proof here when done)

```
$ bash skills/flow/runner/flow.sh coherence | tail -2     # PASS: declared versions agree (0.12.0)
$ bash tests/run_all.sh | tail -1                          # ALL SUITES PASSED
```
World-state: v0.12.0 is coherent across every declared version field and the harness now self-checks
that declared agents are actually wired — verified by `flow.sh coherence` PASS + the tripwire test.
