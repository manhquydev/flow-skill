# C-006 — real wall-clock per-stage dwell in usage analytics

status: done
deps: C-002, C-004
implements: FR3

## Scope

In flow_harness.py `cmd_usage`, compute per-stage wall-clock dwell from existing stage
transitions: group `next` events by `cycle_id` ordered by `epoch_s`, dwell(stage) =
epoch_s(next transition) − epoch_s(this transition). Keep the existing command-exec-time
number but RELABEL both honestly ("stage dwell (wall-clock)" vs "command exec time"). No new
table, no schema change. deps C-002 (needs populated cycle_id to group) and C-004 (harness
cmd_usage already touched there — sequence to avoid conflict).

## Allowed files

- `skills/flow/harness/flow_harness.py` (`cmd_usage` dwell computation + labels)
- `tests/` (regression test)

## Verify (run these before calling the card done)

- [x] On a synthetic cycle with timestamped stage transitions, dwell = wall-clock delta between consecutive stages (not 1-2s exec time).
- [x] Output labels distinguish wall-clock dwell from command exec time.
- [x] Abandoned final stage (no successor) handled without error.
- [x] New test passes; full suite green.

## Done-evidence (world-state proof, named BEFORE building)

Paste `usage` output on a seeded multi-day cycle showing wall-clock dwell per stage (e.g. a
multi-hour/day delta), clearly labeled, plus passing test name.

## Evidence (paste the actual proof here when done)

Synthetic cycle (00-idea→01-research after 1h, →02-scope after +2h):
```
  per-stage command exec time (avg duration_s of 'next' - runner overhead, NOT lead-time):
    00-idea      n=1 avg=0s
  per-stage dwell (wall-clock: avg real time spent IN the stage, from transitions):
    00-idea      n=1 avg=1.0h
    01-research  n=1 avg=2.0h        <- 02-scope (no exit) correctly skipped as abandoned
json stage_dwell(wall): 00-idea=3600s, 01-research=7200s ; stage_exec_time: 0s
```
Implementation: harness `cmd_usage` reconstructs time-in-stage from `next` transitions
(enter=stage_to epoch, exit=stage_from epoch) per cycle; keeps the old exec-time metric but
relabels both honestly; new json keys `stage_dwell` (wall) + `stage_exec_time`. No new table
(uses existing fields, needs C-002's cycle_id). Test `tests/test_flow_usage_log.sh` §13.
Suite: usage_log **41/0**.
