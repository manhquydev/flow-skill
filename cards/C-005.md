# C-005 — Doc-contract regression test + dogfood findings → quality-metrics

status: done
deps: C-001, C-002, C-003, C-004

## Scope

Two things, one cohesive slice (test + the metrics it guards): (a) add
`tests/test_flow_codex_integration.sh` — a regression guard asserting the Codex doc-contract
invariants (installed≠usable, 3-trigger cost gate, gate parity, degrade rule, SKILL pointer) so a
later edit can't silently weaken detect-and-degrade; wire it into `tests/run_all.sh`. (b) record
this run's dogfood findings + the live cross-model catch into `docs/quality-metrics.md`. Then run
the FULL suite to prove 0 regressions (success metric #3).

## Allowed files

- `tests/test_flow_codex_integration.sh` (new)
- `tests/run_all.sh` (edit — register the suite)
- `docs/quality-metrics.md` (edit — findings + version)

## Verify (run these before calling the card done)

- [x] `bash tests/test_flow_codex_integration.sh` exits 0 — 17 passed, 0 failed
- [x] `tests/run_all.sh` includes the new suite (test_flow_codex_integration.sh registered)
- [x] FULL suite green: ALL SUITES PASSED — 241 dev checks (14 prior + 17 codex), 0 regressions vs baseline
- [x] `docs/quality-metrics.md` records 5 dogfood findings (DF-1..DF-5) + the live cross-model catch
- [x] No edits outside Allowed files (test file, run_all.sh, quality-metrics.md)

## Done-evidence (world-state proof, named BEFORE building)

Pasted stdout of `bash tests/run_all.sh` showing ALL SUITES PASSED with the new codex suite among
them; `grep` proving ≥3 dogfood findings recorded in `quality-metrics.md`. The world now has a
regression guard that fails if detect-and-degrade or the cost gate is weakened.

## Evidence (paste the actual proof here when done)

```
$ bash tests/test_flow_codex_integration.sh   # ==== Codex integration doc-contract ====  RESULT: 19 passed, 0 failed
$ bash tests/run_all.sh | tail -3
  RESULT: 19 passed, 0 failed                 # new codex suite among them (19 after review-driven guard tighten)
ALL SUITES PASSED                             # 14 prior suites (224) + codex (19) = 243 dev checks, 0 fail
$ grep -c '^| DF-' docs/quality-metrics.md    # 5 dogfood findings recorded
```
World-state: a regression guard now fails if detect-and-degrade (installed≠usable), the 3-trigger
cost gate, or gate parity is weakened in any of the 5 Codex docs. quality-metrics.md carries the
live cross-model catch (2/2 real defects single-vendor review missed) + 5 dogfood findings — the
"thông số" for the next skill upgrade.
