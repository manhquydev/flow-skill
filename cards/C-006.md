# C-006 — P0 correctness: real liveness probe + auto-run cost-gate parity

status: done
deps: none

## Scope

Fix the two confirmed P0 correctness defects from the adversarial review:
(1) **Liveness probe is non-load-bearing** — `codex-companion.mjs status` returns NO auth field
(verified empirically), so an unauthenticated host passes the USABLE check then fails the real
call. Switch the probe to `codex-companion.mjs setup --json` keyed on `ready===true` /
`auth.loggedIn===true` in codex-integration.md + agent-detection.md.
(2) **Auto-run cost-gate violation** — `auto-run.md` Tier-B routes a FIRST-red repair to billable
Codex without the two-strikes condition. Narrow it: first repair stays same-ladder (fresh Claude
subagent); Codex only at the true 2nd strike / security-class / opt-in. Reference layer only.

## Allowed files

- `skills/flow/references/codex-integration.md`
- `skills/flow/references/agent-detection.md`
- `skills/flow/references/auto-run.md`

## Verify (run these before calling the card done)

- [x] `setup --json` is the documented probe (codex-integration 3, agent-detection 1); `status` no longer the auth check
- [x] Empirical: `status --json` auth field ABSENT; `setup --json` → `auth.loggedIn=True` (probe now real)
- [x] auto-run.md Tier-B: first repair same-ladder; Codex gated to 2nd-strike/security/opt-in (1 match, two-strikes deadlock present)
- [x] No edits outside Allowed files (3 files)

## Done-evidence (world-state proof, named BEFORE building)

`grep` shows `setup --json` as the probe + the corrected auto-run Tier-B wording; pasted live output
proving `setup --json` carries `auth.loggedIn` while `status --json` does not (so the safeguard is now
real); `git diff --name-only` = only the 3 allowed files.

## Evidence (paste the actual proof here when done)

```
$ node codex-companion.mjs status --json | (auth?)   # absent  <- old probe could NOT see auth
$ node codex-companion.mjs setup  --json | .auth.loggedIn   # True  <- new probe is auth-aware
$ grep -c 'setup --json' codex-integration.md agent-detection.md   # 3 ; 1
$ grep -ci 'two-strikes deadlock' auto-run.md   # 1  (Codex gated to the real 2nd strike)
$ git diff --name-only   # codex-integration.md, agent-detection.md, auto-run.md
```
World-state: the USABLE safeguard is now load-bearing (probes the field that actually gates the
billable call); auto-run no longer fires Codex on an ordinary first-red repair — it matches the
3-trigger cost gate (two-strikes deadlock / security-class / opt-in).
