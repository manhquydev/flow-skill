# C-001 - greet
status: done
implements: FR1

## Scope
greet(name) returns a greeting string.

## Independent test
A caller runs greet("Ada") and reads back a greeting naming Ada.

## Allowed files
src/app.py

## Verify (run these before calling the card done)
- [x] greet("Ada") contains "Ada"

## Done-evidence (world-state proof)
the shipped module returns the greeting

## Evidence (paste the actual proof here when done)
verified by running the module
