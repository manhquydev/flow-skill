# C-018 — orchestration completeness (wire docs/git-manager + tighten tripwire + repair rule)

status: done
deps: none
group: v0.12.1-polish
implements: P-4, P-3, P-LOW3

## Scope

Close the orchestration-completeness gaps the v0.12 tripwire surfaced, all docs/test layer:

- **#4 — wire docs-manager + git-manager.** Both are in `agent-detection.md`'s ck: priority list but have NO row in `agent-stage-mapping.md`; the v0.12 tripwire had to grandfather them. Add explicit seam rows: `git-manager` -> the Deploy/ship git step (commit/PR), `docs-manager` -> a docs-sync step after implementation. Honor the portability law (degrade rung: absent -> inline; gate identical).
- **#LOW-3 — tighten the tripwire.** The agent-wiring tripwire's `WIRED_AGENTS` list is static, so a NEW agent added to `agent-detection.md` but left unwired passes silently. Fix: derive the checked set from `agent-detection.md` (parse the priority list) so any declared agent must be wired OR explicitly listed as an accepted exception; keep the negative control. After #4, docs-manager+git-manager move from grandfathered to genuinely-wired.
- **#3 — repair-discipline rule.** Encode in `auto-run.md` (repair ladder): a control-flow / runner repair MUST re-run the FULL `run_all.sh`, not just the targeted suite, before claiming green — and a repair subagent that only ran the targeted suite is BLOCKED, not done. (This is the exact gap that let the C-015 repair regression through.)

## Allowed files

- `skills/flow/references/agent-stage-mapping.md` (git-manager + docs-manager seam rows + degrade note)
- `skills/flow/references/auto-run.md` (the #3 repair-full-suite rule)
- `tests/test_flow_coverage_gaps.sh` (derive checked agents from agent-detection.md; keep negative control; docs/git-manager now required-wired)

## Verify (run before calling the card done)

- [x] `agent-stage-mapping.md` has explicit seam rows for `git-manager` (deploy/ship) and `docs-manager` (docs-sync), each with a degrade rung
- [x] Tripwire now derives its required set from `agent-detection.md` (a new declared-but-unwired agent would go RED) — negative control still proves red-capability
- [x] docs-manager + git-manager are now in the genuinely-wired set (no longer grandfathered)
- [x] `auto-run.md` states: control-flow/runner repairs re-run the FULL suite; targeted-suite-only repair = BLOCKED
- [x] No edits outside Allowed files
- [x] `bash tests/run_all.sh` -> ALL SUITES PASSED

## Done-evidence (world-state proof, named BEFORE building)

`grep -nE 'git-manager|docs-manager' skills/flow/references/agent-stage-mapping.md` returns the new
seam rows; the coverage-gaps tripwire derives its set from agent-detection.md and stays green with a
working negative control; `auto-run.md` contains the repair-full-suite rule. `bash tests/run_all.sh`
ends ALL SUITES PASSED.

## Evidence (paste actual proof here when done)

```
agent-stage-mapping.md: git-manager (Deploy/ship) + docs-manager (Docs sync) seam rows added
coverage_gaps tripwire: derives required set from agent-detection.md ck: list (not static);
  docs-manager + git-manager now genuinely-wired; negative control still red-capable
auto-run.md: repair-discipline rule added (control-flow/runner repair re-runs FULL suite; else BLOCKED)
bash tests/test_flow_coverage_gaps.sh -> 30 passed, 0 failed ; run_all -> ALL SUITES PASSED
```
Adversarial review: APPROVE (all 3 scope items correct; negative-control form-weakness is pre-existing).
