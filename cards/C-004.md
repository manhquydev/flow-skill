# C-004 — Surface wiring: SKILL.md + READMEs + version coherence (v0.4.0)

status: done
deps: C-001, C-002, C-003

## Scope

Make the Codex tier visible at the skill's surface: SKILL.md §Agent orchestration mentions the
Codex cross-vendor tier + points to `codex-integration.md`; README.md + README_VN.md update the
one-line "agent orchestration (ck: + bmad)" to include Codex. Fix the version-coherence drift
(assessment risk #2): bump SKILL.md `0.2.0` → `0.4.0` to match portable-manifest (also → 0.4.0)
and this feature. No runner edits, no reference-doc edits (those are C-001..C-003).

## Allowed files

- `skills/flow/SKILL.md` (edit)
- `README.md` (edit)
- `README_VN.md` (edit)
- `portable-manifest.json` (edit — version)

## Verify (run these before calling the card done)

- [x] `grep -ci codex skills/flow/SKILL.md` = 4 and points to `codex-integration.md` (2 refs)
- [x] README.md + README_VN.md agent-orchestration line names Codex (1 each)
- [x] Version coherent: SKILL.md + portable-manifest.json both `0.4.0` (manifest valid JSON). NOTE: `flow.sh coherence` returned "no declared version fields found — skipped" → it failed to detect these fields (dogfood finding DF-3, captured in C-005)
- [x] No edits outside Allowed files (SKILL.md, README.md, README_VN.md, portable-manifest.json)

## Done-evidence (world-state proof, named BEFORE building)

`grep` shows Codex named in SKILL.md (with the `codex-integration.md` pointer) and in both READMEs'
orchestration lines; `flow.sh coherence` reports no version drift between SKILL.md and
portable-manifest (both 0.4.0); `git diff --name-only` shows only the 4 allowed files.

## Evidence (paste the actual proof here when done)

```
$ grep -ci codex skills/flow/SKILL.md ; grep -c codex-integration skills/flow/SKILL.md   # 4 ; 2
$ grep -ci codex README.md README_VN.md         # README.md:1  README_VN.md:1
$ grep -hoE '0\.[0-9]\.[0-9]' skills/flow/SKILL.md portable-manifest.json   # 0.4.0 / 0.4.0
$ node -e "JSON.parse(fs.readFileSync('portable-manifest.json'))"           # valid JSON
```
World-state: the skill surface now advertises the Codex tier (SKILL.md §Agent orchestration +
Law/reference list pointer; both READMEs' orchestration line; bilingual). Version coherence drift
(assessment risk #2) fixed: SKILL.md + manifest both 0.4.0. README status bumped v0.3 → v0.4.
