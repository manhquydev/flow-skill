# PRD - task list service

## Features
- FR1: a user creates a task via POST /tasks and sees it in GET /tasks.
- FR2: a user marks a task done via POST /tasks/{id}/done and it then reads as done.

## Success metric
- 95% of task creates succeed in under 300ms.
