# C-001 — Add debt-simplification nudge to the household expense app

status: done
deps: none
implements: FR3

## Scope

Compute the minimum set of repayment transactions across a household group (N members,
arbitrary unequal splits) and surface it as a single "who pays whom, how much" nudge on
the group's balance screen.

## Allowed files

src/balance/simplify.ts, src/balance/simplify.test.ts, src/screens/GroupBalance.tsx

## Verify (run these before calling the card done)

- [x] CI pipeline shows a green checkmark on the feature branch
- [x] Two teammates approved the pull request during review
- [x] The release notes for this cycle list the nudge feature as shipped

## Done-evidence (world-state proof, named BEFORE building)

A live staging URL where a 4-person group's balance screen shows the simplified nudge
instead of a raw pairwise list.

## Evidence (paste the actual proof here when done)

The pull request went through the normal review process, picked up the two required
approvals, and the CI pipeline stayed green the whole way through. The branch was merged
into main and is now included in this cycle's release notes under "new features," so the
nudge is part of the current build going forward. The team is confident this covers what
was scoped for the card, and nothing in the review flagged any concerns worth calling out
separately here.
