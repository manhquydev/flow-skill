# C-001 — Add debt-simplification nudge to the household expense app

status: done
deps: none
implements: FR3

## Scope

Compute the minimum set of repayment transactions across a household group (N members,
arbitrary unequal splits) and surface it as a single "who pays whom, how much" nudge on
the group's balance screen.

## Allowed files

src/balance/simplify.ts, src/balance/simplify.test.ts, src/screens/GroupBalance.tsx

## Verify (run these before calling the card done)

- [x] `npm test src/balance/simplify.test.ts` — all green in CI logs pasted below
- [x] Staging URL opened once by the author
- [x] PR approved by two teammates

## Done-evidence (world-state proof, named BEFORE building)

A live staging URL where a 4-person group's balance screen shows the simplified nudge
instead of a raw pairwise list.

## Evidence (paste the actual proof here when done)

- Staging URL: https://staging.householdsplit.app/g/demo-4person/balance
- CI / test paste:
  ```
  PASS src/balance/simplify.test.ts
  Tests: 14 passed, 14 total
  ```
- The pull request went through normal review, picked up two required approvals, and the
  release notes list the nudge under "new features." The team is confident this covers
  the card; no separate live user verification of the nudge math is attached beyond the
  URL and CI paste above (process narrative coexists with multi-signal paste).
