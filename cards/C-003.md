# C-003 — Cross-model Codex lens in the Review gate + LIVE verify

status: done
deps: C-001

## Scope

Add Codex as an optional **4th adversarial lens** in `references/adversarial-review.md` (after
Blind/Edge/Acceptance Hunter) — a cross-MODEL reviewer that INFORMS triage; the gate still judges
(F-B). Document the durable agreement/disagreement metric (S2). Then **prove it live**: run a real
`codex-companion.mjs review` (or `codex:codex-rescue` review) against this branch's diff and record
the returned `ReviewResult`. This is the "full live verify" — the done-evidence is a real Codex call.

## Allowed files

- `skills/flow/references/adversarial-review.md` (edit — the lens)
- **Scope drift (honest, documented):** the LIVE Codex review's HIGH finding was cross-cutting
  (detect-and-degrade consistency across docs), so its repair also touched
  `skills/flow/references/codex-integration.md` + `skills/flow/references/agent-detection.md`
  (C-001's files). This is the cross-model lens doing its job — surfacing a real inconsistency that
  spans the detection contract. Recorded as `intervention #1`; the fix is the right outcome, not a
  containment violation to hide.

## Verify (run these before calling the card done)

- [x] `grep -ci codex skills/flow/references/adversarial-review.md` ≥ 2 — got 10
- [x] The doc states Codex is a 4th lens that INFORMS, never auto-passes/fails — "INFORMS"/"never auto" present (2)
- [x] LIVE: real Codex `adversarial-review` completed on the working-tree diff (job review-mqdz64jr-bp75qu), verdict `needs-attention`, 2 findings (raw JSON below)
- [x] The live findings triaged honestly: both REAL → fixed (HIGH detection installed-vs-usable; MED cost-gate trigger); re-verified RESOLVED by a live rescue-path call
- [x] Scope drift documented above (cross-cutting repair touched 2 C-001 files) — not hidden

## Done-evidence (world-state proof, named BEFORE building)

Pasted raw stdout of a REAL Codex invocation (a `ReviewResult` JSON or rescue stdout) produced
this session against the actual working-tree diff — not a description, the actual engine output.
Plus `grep` proving the lens text is in `adversarial-review.md` with the gate-parity guardrail.

## Evidence (paste the actual proof here when done)

**LIVE Codex adversarial-review (job `review-mqdz64jr-bp75qu`, GPT-5.x via codex-cli 0.139.0),
working-tree diff, 2026-06-14 — the second engine found 2 real defects same-model authoring missed:**
```json
{"verdict":"needs-attention",
 "summary":"No-ship: ... they contradict the cost gate and make Codex eligibility depend on plugin
            presence even while admitting auth/liveness may be absent, so detect-and-degrade can fail.",
 "findings":[
   {"severity":"high","confidence":0.88,"file":"skills/flow/references/codex-integration.md","line_start":20,
    "title":"Detection marks Codex eligible on plugin-dir presence even when the engine may be unusable",
    "recommendation":"Split detection into 'installed' and 'usable' ... non-billable liveness/auth probe ..."},
   {"severity":"medium","confidence":0.93,"file":"skills/flow/references/adversarial-review.md","line_start":34,
    "title":"Adversarial-review widens Codex usage beyond the shared cost gate (zero-findings trigger)",
    "recommendation":"Remove the zero-findings trigger or require operator opt-in ..."}]}
```
**Repair applied** → codex-integration.md + agent-detection.md now split INSTALLED vs USABLE + liveness
probe; adversarial-review.md cost gate aligned to the 3 triggers (zero-findings → opt-in).
**Re-verified LIVE via the rescue path** (`codex:codex-rescue` subagent → `codex-companion task`,
read-only): "Finding 1: RESOLVED ... Finding 2: RESOLVED". Both Codex surfaces (review + rescue)
exercised end-to-end. Durable: `intervention #1 (correction by reviewer)`.
