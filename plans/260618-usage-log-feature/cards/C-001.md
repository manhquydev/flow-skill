# C-001 — Mechanical usage log: _log_event + EXIT trap + dual sink + mask + cycle_id

status: done
deps: none
implements: FR1, FR2, FR3, FR4, FR5

## Scope

Add the mechanical capture core to the SOURCE runner `skills/flow/runner/flow.sh`:
- `_log_event` (I1): build the event JSON from globals + `_now()`; append FULL line to `.flow/events.jsonl`, COMPACT line to `~/.claude/flow/usage.jsonl`. Best-effort, returns 0 always.
- `_mask_secrets` (I3): denylist regex → `***` (per contract denylist).
- `_log_on_exit` + `trap '_log_on_exit' EXIT` set once near top (I2): capture `$?` on FIRST line, compute `duration_s` from a `FLOW_START_S=$(_now)` set at startup, set `FLOW_GATE_PASS` (true/false for next|check, else empty/null), call `_log_event`, re-exit original `$?`.
- Cycle stamp (I4): in `cmd_next`, when stage 00 unlocks, write `.flow/cycle_id` = `<epoch_s>-<short-host>` (best-effort); `_log_event` reads it.
- `read_only` classification + disable no-op (I11): `FLOW_LOG_DISABLE=1` OR `DO_NOT_TRACK=1`.
- Compact global line kept <PIPE_BUF (drop args/host/stage_from).

NOT in this card: migration 006, rollup, `flow usage` (→ C-002); tests/docs/version (→ C-003).

## Allowed files

- `D:\project\flow\flow-skill\skills\flow\runner\flow.sh` (the SOURCE copy — NOT the installed running runner)

## Verify (run these before calling the card done)

- [x] `bash skills/flow/runner/flow.sh status` in a scratch project → `.flow/events.jsonl` gains 1 line; `jq .` parses it; `command=="status"`, `read_only==true`, `exit_code==0`.
- [x] `~/.claude/flow/usage.jsonl` gains a compact line (no `args` key), length <4096 bytes.
- [x] A run with a denylist arg (e.g. `harness intake --summary "api_key=AKIAX"` style token) → written line shows `***`, no plaintext token. (grep the line)
- [x] No-fail: make a sink dir unwritable (or point `HOME` at a read-only path) → `flow.sh status` still exits 0 with normal output.
- [x] `FLOW_LOG_DISABLE=1 bash ... status` → no new line appended; same for `DO_NOT_TRACK=1`.
- [x] Portability scan: `grep -nE '\\b|[^%]%N|grep -P|date \+%[^s%]' ` over the added lines → 0 GNU-only constructs.

## Done-evidence (world-state proof, named BEFORE building)

A real `flow.sh` invocation on this device produces a parseable JSON event in BOTH `.flow/events.jsonl` (full) and `~/.claude/flow/usage.jsonl` (compact), with exit code unchanged — pasted real file contents + the no-fail run output.

## Evidence (paste the actual proof here when done)

Verified by running the SOURCE runner (`skills/flow/runner/flow.sh`) in scratch projects with `HOME` isolated to `/tmp/flog-home` (real global log untouched). `bash -n` clean.

**1. `status` writes a parseable FULL event (per-project `.flow/events.jsonl`):**
```
{"ts":"2026-06-18T03:04:09Z","epoch_s":1781751849,"session_id":"test-c001","cycle_id":"","project":"flow-c001","command":"status","args":"","exit_code":0,"gate_pass":null,"duration_s":0,"stage_from":"","stage_to":"","card":"","project_type":"web","mode":"","flow_version":"0.8.0","tier":"builtin","host":"manhquy","read_only":true}
```
parse: 19 keys, command=status, read_only=true, exit_code=0.

**2. COMPACT global line (`~/.claude/flow/usage.jsonl`) — no `args` key, 228 bytes (<4096 PIPE_BUF):**
```
{"ts":"2026-06-18T03:04:09Z","epoch_s":1781751849,"session_id":"test-c001","cycle_id":"","project":"flow-c001","command":"status","exit_code":0,"gate_pass":null,"duration_s":0,"stage_to":"","flow_version":"0.8.0","read_only":true}
```

**3. Masking — `harness --summary "api_key=AKIA12345SECRETVALUE"`:** written `args` field = `***redacted***`; leak check `'AKIA12345' in args` → False.

**4. No-fail — `.flow/events.jsonl` made a directory (append must fail):** `flow.sh status` exit = **0**, output intact.

**5. Disable — `FLOW_LOG_DISABLE=1` and `DO_NOT_TRACK=1`:** line count unchanged (before=2, after=2).

**6. `next` greenfield — cycle stamp + stage carry:** `.flow/cycle_id` = `1781751983-manhquy`; event `command=next, stage_to="00-idea", gate_pass=true, cycle_id set`.

**7. Portability:** fixed-string scan of added code (1313–1374) → 0 GNU-only constructs (the `grep -P` at 783/1168/1170 and `\b` at 1083 are PRE-EXISTING, BSD-guarded). `bash -n` OK.

