# C-003 — tests + docs + version bump (0.10.0)

status: done
deps: C-001, C-002
implements: none (gate parity / no-regression card)

## Scope

Lock the v2 loop-closing features against regression and document them:
- Extend `tests/test_flow_usage_log.sh` (or a sibling suite) with checks: `usage --summary` one-line digest + empty-on-no-data (FR1/I1); recall shows the USAGE block + identical output when `FLOW_HARNESS_DISABLE=1` (FR1/I2, no-fail); `propose` emits a stage-fail proposal from seeded high-fail data (FR2); `prune --keep K` leaves exactly K last lines + dropped count + idempotent re-run (FR3); failing `next` records non-empty `gate_fail_reason` and passing records empty, **exit codes unchanged** (FR4); migration 007 → schema_version 7.
- **Portability scan** for GNU-only constructs in the new runner code.
- Run the FULL existing suite → 0 regressions.
- Docs: update `skills/flow/harness/README.md` (usage-log section: recall summary + propose branch + prune + gate_fail_reason) + `SKILL.md` (`/flow usage --prune` note; recall now surfaces usage) + README/README_VN status line.
- Version bump 0.9.0 → 0.10.0 across the 3 declared sources; `flow.sh coherence` clean.

NOT here: behavior changes to C-001/C-002 (if a test fails, fix in the owning card).

## Allowed files

- `D:\project\flow\flow-skill\tests\test_flow_usage_log.sh` (+ `tests\run_all.sh` if a new suite file is added)
- `D:\project\flow\flow-skill\skills\flow\harness\README.md`
- `D:\project\flow\flow-skill\skills\flow\SKILL.md`
- `D:\project\flow\flow-skill\README.md`, `README_VN.md`
- version files: `portable-manifest.json`, `.claude-plugin/plugin.json`

## Verify (run these before calling the card done)

- [x] `bash tests/run_all.sh` → ALL pass, including the new usage-loop checks; suite count ≥ prior baseline.
- [x] Portability scan → 0 GNU-only constructs in added runner code.
- [x] `bash flow.sh coherence` (at repo root) → all declared versions agree at 0.10.0.

## Done-evidence (world-state proof, named BEFORE building)

Full installed-skill test run on this device green (pasted summary with suite/test counts), portability scan clean, and `coherence` reports 0.10.0 agreement — pasted real outputs.

## Evidence (paste the actual proof here when done)

**Extended suite `tests/test_flow_usage_log.sh` (now 27 checks, +8 v2 part-8):**
```
8) v2 loop-closing: migration 007 + usage --summary + recall block + gate-reason + prune + propose
  ok  migration 007 applied (schema_version 7)
  ok  usage --summary prints one-line digest
  ok  usage --summary silent on no data
  ok  recall surfaces the usage block
  ok  recall omits usage block when harness disabled
  ok  failing gate records gate_fail_reason
  ok  usage --prune caps to keep N
  ok  propose surfaces a stage-fail proposal from the usage log
RESULT: 27 passed, 0 failed
```

**Full suite (registered), clean run, 0-regression:**
```
ALL SUITES PASSED
GRAND: 394 passed, 0 failed across 20 suites      # 386 + 8 new = 394
```

**Version + coherence (no drift):**
```
coherence check - declared versions:
  0.10.0  .claude-plugin/plugin.json
  0.10.0  portable-manifest.json
  0.10.0  skills/flow/SKILL.md
  PASS: declared versions agree.
```
0.9.0 → 0.10.0.

**Docs:** `harness/README.md` gained a "Closed feedback loop (schema 007)" section; `SKILL.md` `/flow usage` row updated (`--prune`, recall digest, propose); README EN/VN status → v0.10.0 loop-closed.

