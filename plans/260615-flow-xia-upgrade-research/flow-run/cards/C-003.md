# C-003 — assess repo-map symbol ranking (optional tree-sitter)

status: done
deps: none
implements: FR3

## Scope

Enhance `flow.sh assess` so `flow/00-inspect.md` is seeded with a ranked repo-map — symbols ranked
by reference count (Aider-style), most-referenced first — when `tree-sitter` is importable, and
falls back to the existing flat glob scan (with a "ranking unavailable" note, no error) when it is
not. ONE thing: the ranked repo-map + its graceful fallback. **tree-sitter stays OPTIONAL** (never
a required dep). Repo root: `D:\project\flow\flow-skill`.

## Allowed files

- `skills/flow/harness/repo_map.py` (new — optional tree-sitter ranker + fallback signal)
- `skills/flow/runner/flow.sh` (`cmd_assess` wiring + merge + glob fallback)
- `skills/flow/_templates/00-inspect.md` ("Ranked surfaces" section)
- `skills/flow/SKILL.md` / assess docs (note optional dep + degradation)
- `skills/flow/tests/test_flow_assess.sh` (with/without tree-sitter), `skills/flow/tests/run_all.sh`

## Verify (run these before calling the card done)

- [x] With no rankable source: `assess` exits 0, prints a graceful note, flat scan still seeds `00-inspect.md` (no error). (suite F)
- [x] With a real src tree: the widely-referenced file appears in the ranked surfaces of `00-inspect.md`. (suite E)
- [x] `repo_map.py` is best-effort (size cap, OSError-guarded, prints nothing → exit 0 when nothing rankable). (suite F)
- [x] New `test_flow_assess.sh` cases (E/F/G) green; full `run_all.sh` green, 0 regressions.

(Design refined per inspect-first: tree-sitter would never run unconfigured, so the ranker is a
**stdlib reference-count ranker** — strictly more portable, no heavy dep. Contract amended first.)

## Done-evidence (world-state proof, named BEFORE building)

Pasted output showing: (a) `assess` run with no tree-sitter → exit 0 + fallback note + populated
`00-inspect.md`, (b) `assess` run with tree-sitter → ranked surfaces section with the expected
top-ranked file, (c) `run_all.sh` all-green summary. World-state = the installed `assess` behaving
on a real tree + green rig.

## Evidence (paste the actual proof here when done)

Real ranker output on the flow-skill repo itself (high-leverage code surfaces first):

```
$ python skills/flow/harness/repo_map.py . 5
1. skills/flow/runner/flow.sh        (score 128; scan_gate, planning_complete, _python)
2. skills/flow/harness/_db.py        (score 64; rows, update, insert)
3. dogfood/cmc-odoo-assess.workflow.js (score 51; records, known, VENDORED)
4. skills/flow/harness/flow_harness.py (score 42; _jlist, _order_by_reuse, _touch_accessed)
5. skills/flow/harness/_domain.py     (score 6; normalize_flags, score_trace, classify_lane)

$ bash tests/test_flow_assess.sh   -> RESULT: 20 passed, 0 failed  (E ranks a real src tree,
                                       F graceful no-source, G excludes non-unique + stopword names)
$ bash tests/run_all.sh            -> ALL SUITES PASSED (0 regressions)
```

Cross-model review (Codex/GPT-5.x) found 5 real issues the suites missed: (1) no file-size cap;
(2) O(symbols×corpus) regex perf; (3) generic stopword symbols inflated scores; (4) non-unique
symbol names corrupted ranking; (5) `_python` could pick a py2 interpreter. ALL FIXED (single-pass
tokenization, 512KB cap, stopwords + min-length, skip-non-unique, py3-verified `_python`) and locked
with suite case G.
