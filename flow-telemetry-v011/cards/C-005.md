# C-005 — enrich device-global log with bounded gate_fail_reason

status: done
deps: C-004
implements: FR6

## Scope

Add a truncated `gate_fail_reason` (≤120 chars) to the COMPACT device-global log line in
flow.sh `_log_event`, so device-wide gate failures are explainable. Keep the line small
(interim approach; no flock, no shards). Ensure flow_harness.py rollup/usage tolerate the new
field and can surface it. deps C-004 because both edit the same `_log_event` global printf —
build after C-004 to avoid a conflicting edit.

## Allowed files

- `skills/flow/runner/flow.sh` (`_log_event` compact printf only)
- `skills/flow/harness/flow_harness.py` (ingest/tolerate field if needed)
- `tests/` (regression test)

## Verify (run these before calling the card done)

- [x] After a real gate failure in a temp project, the device-global line carries a non-empty, truncated `gate_fail_reason`.
- [x] Global line stays small (well under 4096 bytes); truncation works for long reasons (cut -c1-120).
- [x] Old global lines without the field still roll up (harness reads it as optional; verified by full suite).
- [x] New test passes; full suite green.

## Done-evidence (world-state proof, named BEFORE building)

Paste a device-global JSON line from a forced gate failure showing the truncated
`gate_fail_reason`, plus byte-length check, plus passing test name.

## Evidence (paste the actual proof here when done)

Forced gate failure → device-global compact line now explains it:
```
{...,"command":"next","exit_code":1,"gate_pass":false,...,"gate_fail_reason":"fill:4,unchecked:3","ephemeral":0}
compact line length: 339 bytes  (ceiling 4096)
```
Implementation: flow.sh computes `gfr` = `FLOW_LAST_GATE_FAIL` truncated to 120 chars and
adds `gate_fail_reason` to the COMPACT global printf (the FULL per-project line already had
it). Test `tests/test_flow_usage_log.sh` §12. Suite: usage_log **38/0**.
