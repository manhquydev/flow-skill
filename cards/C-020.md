# C-020 — v0.12.1 release: polish round coherence

status: done
deps: C-017, C-018, C-019
group: release
implements: P-REL

## Scope

Cut v0.12.1 (patch — the polish round closing the v0.12 backlog: telemetry-honesty labels,
orchestration completeness, engine hygiene). Bump every declared version 0.12.0 -> 0.12.1
(`skills/flow/SKILL.md`, `portable-manifest.json`, `.claude-plugin/plugin.json`) + README status
lines; add a `0.12.1` CHANGELOG entry listing the closed backlog items (legacy-dwell approx label,
dead-var, docs/git-manager wiring + tightened tripwire, repair-full-suite rule, tempdir EXIT trap);
update `docs/quality-metrics.md` with the v0.12.1 run + test counts. `/flow coherence` must PASS.
Doc/manifest layer only.

## Allowed files

- `skills/flow/SKILL.md` · `portable-manifest.json` · `.claude-plugin/plugin.json`
- `README.md` · `README_VN.md` · `CHANGELOG.md` · `docs/quality-metrics.md`

## Verify (run before calling the card done)

- [x] All declared versions = `0.12.1` (SKILL.md, portable-manifest.json, plugin.json)
- [x] `flow.sh coherence` → PASS
- [x] CHANGELOG has a `0.12.1` entry covering C-017/C-018/C-019
- [x] READMEs + quality-metrics updated (version + test counts)
- [x] No edits outside Allowed files
- [x] `bash tests/run_all.sh` → ALL SUITES PASSED

## Done-evidence (world-state proof, named BEFORE building)

`flow.sh coherence` prints PASS at 0.12.1; CHANGELOG has the 0.12.1 entry; `bash tests/run_all.sh`
ends ALL SUITES PASSED with the count recorded in quality-metrics.

## Evidence (paste actual proof here when done)

```
flow.sh coherence -> PASS: declared versions agree (0.12.1 in SKILL.md/plugin.json/manifest)
CHANGELOG: 0.12.1 entry (C-017 telemetry-honesty, C-018 orchestration completeness, C-019 hygiene)
README + quality-metrics corrected to live counts: 20 suites / 467 checks (v0.12.0 had miscounted "21")
bash tests/run_all.sh -> ALL SUITES PASSED (20 suites / 467 / 0 failed)
```
