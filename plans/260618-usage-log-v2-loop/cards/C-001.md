# C-001 — harness: usage --summary + usage→propose branch + prune + migration 007

status: done
deps: none
implements: FR1 (I1), FR2 (I3), FR3 (I4), FR4 (I5 schema)

## Scope

The data/query layer in `skills/flow/harness/`:
- **Migration `schema/007-usage-gate-reason.sql`** (I5): `ALTER TABLE usage_event ADD COLUMN gate_fail_reason TEXT;` + `INSERT INTO schema_version (version) VALUES (7);`. Add `gate_fail_reason` to `USAGE_COLS` in `flow_harness.py` (back-compatible: `.get` → None for old lines).
- **`usage --summary`** (I1): a compact one-block mode of `cmd_usage` — `USAGE (mechanical log): cycles=.. reached-cards=.. | cycle-time s min/med/max=.. | gate fail-rate=..% | top-fail-stage=..(f/t)`. Empty string when no rows. (rolls up first, like plain `usage`.)
- **`_build_proposals` usage branch** (I3): for each `stage_to` over `next|check` events, if fail-rate ≥ 0.5 AND the stage failed in ≥ 2 distinct `cycle_id` → emit a proposal `{title, component=stage, current_pain "f/t over k cycles", suggested "tighten artifact/template or split stage", confidence}`. Heuristic, surfaced (operator commits via `propose --commit`); never auto-applied.
- **`prune`** (I4): `flow_harness.py prune [--keep N] [--global]` — keep last N lines of each sink via temp-write + `os.replace` (atomic, crash-safe); print `{"sink":..,"kept":..,"dropped":..}` per sink. Default keep 5000.

NOT here: the flow.sh recall hook / `usage --prune` dispatch / gate-fail shell capture (→ C-002); tests/docs/version (→ C-003).

## Allowed files

- `D:\project\flow\flow-skill\skills\flow\harness\schema\007-usage-gate-reason.sql` (new)
- `D:\project\flow\flow-skill\skills\flow\harness\flow_harness.py`

## Verify (run these before calling the card done)

- [x] Migration applies: `python flow_harness.py init` then `sqlite3 .flow/harness.db 'select max(version) from schema_version'` → 7; `usage_event` has column `gate_fail_reason`.
- [x] `python flow_harness.py usage --summary` on a project with events → prints the single `USAGE (mechanical log): ...` line; on an empty project → prints nothing (empty), exit 0.
- [x] Seed a stage failing ≥2 cycles ≥50% → `python flow_harness.py propose` lists a stage-fail proposal naming that stage; `--commit` writes a backlog row (`query backlog`).
- [x] `python flow_harness.py prune --keep 3` on a sink with 10 lines → reports `dropped:7,kept:3`; file now has exactly 3 lines (the LAST 3); re-run → dropped:0.

## Done-evidence (world-state proof, named BEFORE building)

On this device: `usage --summary` prints a real one-line digest, `propose` surfaces a real stage-fail proposal from seeded data, `prune --keep K` leaves exactly K lines, and migration 007 takes `schema_version` to 7 — pasted real command outputs.

## Evidence (paste the actual proof here when done)

Verified against the SOURCE harness, `/d/`-rooted scratch (Git Bash ↔ Windows-python path agreement), seeded events (2 cycles, stage 03-prd failing in both). The harness executed all subcommands → file is valid Python (the `ast.parse` FileNotFoundError is the Windows-python `/d/` open quirk, not a code error).

**Migration 007:** `schema_version = 7`; `has gate_fail_reason = True`.

**`usage --summary`** (one block, for recall):
```
USAGE (mechanical log): cycles=2 reached-cards=1 | cycle-time s min/med/max=10/20/20 | gate fail-rate=40% | top-fail-stage=03-prd(2/3)
```
Empty project → **no output** (silent, exit 0).

**`propose` usage branch:** `[medium] Stage 03-prd fails its gate often` (fail-rate 2/3 over 2 cycles).

**`prune --keep 3`** on a 5-line sink:
```
{"sink": ".../events.jsonl", "kept": 3, "dropped": 2}     # then file has exactly 3 lines (the last 3)
{"sink": ".../events.jsonl", "kept": 3, "dropped": 0}     # idempotent re-run
```
Prune also resets `usage_event`+`rollup_cursor` for that sink (line numbers changed) so the next rollup re-ingests cleanly — proactive fix for the prune↔cursor interaction.

