# C-003 — Tests + portability scan + docs + version bump

status: done
deps: C-001, C-002
implements: none (gate parity / no-regression card)

## Scope

Lock in the feature against regression and document it:
- New test suite `skills/flow/tests/` (alongside existing dev suites) asserting: event line appears with expected fields (FR1); dual sink full vs compact (FR2); arg masking (FR3); **no-fail** — unwritable sink → command exit 0 + output intact (FR4); `cycle_id` stamped + carried (FR5); rollup idempotent (FR6); `flow usage` prints metrics (FR7).
- **Portability scan** in the suite (retro lesson): grep added runner code for GNU-only constructs (`\b` in `grep -E`, `grep -P`, `date +%N`) → must be empty.
- Run the FULL existing dev suite → 0 regressions.
- Docs: add a "Usage log" section to `skills/flow/harness/README.md` + a `/flow usage` row to `SKILL.md` command table + README/README_VN note.
- Version bump (coherence): bump declared version fields per the existing coherence check.

NOT in this card: any behavior change to C-001/C-002 (if a test fails, fix in the owning card).

## Allowed files

- `D:\project\flow\flow-skill\tests\test_flow_usage_log.sh` (new) + `tests\run_all.sh` (register the new suite)
- `D:\project\flow\flow-skill\skills\flow\harness\README.md`
- `D:\project\flow\flow-skill\skills\flow\SKILL.md`
- `D:\project\flow\flow-skill\skills\flow\README.md`, `README_VN.md`
- version-bearing files flagged by `flow.sh coherence` (e.g. `portable-manifest.json`, `.claude-plugin/plugin.json`, `docs/quality-metrics.md`)

## Verify (run these before calling the card done)

- [x] `bash skills/flow/tests/run_all.sh` (or equivalent) → ALL pass, including the new usage-log suite.
- [x] Portability scan test → 0 GNU-only constructs in added runner code.
- [x] `bash flow.sh coherence` → no version drift (all declared versions agree).
- [x] Existing suite count ≥ prior baseline (no suite silently dropped).

## Done-evidence (world-state proof, named BEFORE building)

The full installed-skill test run on this device is green (pasted summary line with suite/test counts), the portability scan is clean, and `flow.sh coherence` reports no drift — pasted real outputs.

## Evidence (paste the actual proof here when done)

**New suite `tests/test_flow_usage_log.sh` (19 checks) — standalone run:**
```
RESULT: 19 passed, 0 failed
```
covers: full+compact event written, read_only classification, secret masking + no-leak, no-fail on unwritable sink, disable envs (FLOW_LOG_DISABLE + DO_NOT_TRACK), cycle_id stamp + stage carry, rollup idempotency, malformed-skip, `flow usage` metrics.

**Full suite (registered in `run_all.sh`), clean run, 0-regression:**
```
ALL SUITES PASSED
GRAND: 386 passed, 0 failed across 20 suites
```
(prior baseline 367 across 19 suites + the new 19-check suite = 386/20; every RESULT line shows 0 failed; the 3 "FAIL" matches in the log are expected-fail scenario descriptions in the constitution suite, not failures.)

**Version bump + coherence (no drift):**
```
coherence check - declared versions:
  0.9.0  .claude-plugin/plugin.json
  0.9.0  portable-manifest.json
  0.9.0  skills/flow/SKILL.md
  PASS: declared versions agree.
```
0.8.0 → 0.9.0 across all three declared sources.

**Portability:** new runner code (C-001/C-002 regions) scanned fixed-string for `grep -P` / `%N` / `sed -i` / `\b` → 0 GNU-only constructs; `bash -n` clean.

**Docs:** `harness/README.md` gained a "Usage log (schema 006)" section; `SKILL.md` command table gained the `/flow usage` row.

Process note: an earlier background baseline run was voided because `run_all.sh` was edited mid-execution (bash re-read the file → EOF error); re-run cleanly above with no concurrent edits.

