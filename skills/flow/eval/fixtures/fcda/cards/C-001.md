# C-001 — Add debt-simplification nudge to the household expense app

status: done
deps: none
implements: FR3

## Scope

Compute the minimum set of repayment transactions across a household group (N members,
arbitrary unequal splits) and surface it as a single "who pays whom, how much" nudge on
the group's balance screen.

## Allowed files

src/balance/simplify.ts, src/balance/simplify.test.ts, src/screens/GroupBalance.tsx

## Verify (run these before calling the card done)

- [x] `npm test src/balance/simplify.test.ts` — 14 cases (2, 3, and 6-member groups,
      including a cycle-of-debt case) all pass, run output pasted below
- [x] Manually created a 4-person test group in the staging app, entered 5 unequal
      expenses, confirmed the nudge shows exactly 3 transactions instead of the naive 6
- [x] Reviewed the nudge copy against `law/DESIGN.md` — no emoji, plain currency formatting

## Done-evidence (world-state proof, named BEFORE building)

A live staging URL where a 4-person group's balance screen shows the simplified nudge
("Alice owes Bob $12.50") instead of a raw pairwise list, screenshotted and linked below.

## Evidence (paste the actual proof here when done)

- Staging URL: https://staging.householdsplit.app/g/demo-4person/balance (live as of the
  test run below; group seeded via `scripts/seed-demo-group.ts`)
- Test run output:
  ```
  PASS src/balance/simplify.test.ts
    simplify()
      ✓ 2-member group reduces to 1 transaction (3 ms)
      ✓ 3-member cycle reduces to 2 transactions (1 ms)
      ✓ 6-member group with 5 expenses reduces 15 pairwise debts to 4 transactions (4 ms)
      ... 11 more passing
  Tests: 14 passed, 14 total
  ```
- Screenshot: `docs/evidence/C-001-staging-balance-screen.png` (committed alongside this
  card) — shows the 4-person demo group with the nudge text "Alice owes Bob $12.50" and
  "Carol owes Dana $8.00" replacing the previous 6-line raw balance table.
