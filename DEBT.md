# DEBT - deliberate gate-skips (a loan, written down)

- [ ] DEBT: macOS-only: eval harness _run_with_timeout fallback does not bound a slow/stuck claude call -- billable eval verb could run an API call past --timeout on macOS (bash 3.2 stock /bin/sh), unbounded cost risk; ubuntu+windows unaffected (both have real timeout/gtimeout); 3 targeted fixes tried against real 3-OS CI (bash-3.2 empty-array _cleanup_tds crash - fixed a separate real bug; bare wait vs wait $PID; cat|claude pipe vs direct < file redirect) none resolved this specific one; needs actual macOS access to diagnose further (kill -TERM $pid not reaching the mock/real claude process, or wait $pid not unblocking on signal-killed child, exact mechanism unconfirmed) -- close before: before running eval for real against untrusted/slow prompts on macOS, or before relying on --timeout as a hard cost cap on macOS -- opened 2026-07-10
