# C-001 — Add debt-simplification nudge to the household expense app

status: done
deps: none
implements: FR3

## Scope

Compute the minimum set of repayment transactions across a household group (N members,
arbitrary unequal splits) and surface it as a single "who pays whom, how much" nudge on
the group's balance screen.

## Allowed files

src/balance/simplify.ts, src/balance/simplify.test.ts, src/screens/GroupBalance.tsx

## Verify (run these before calling the card done)

- [x] Simplify cases for 2, 3, and 6-member groups all came back PASS
- [x] A 4-person demo group now settles in three transfers instead of the naive six
- [x] Nudge copy reviewed against the design law — no emoji, plain currency formatting

## Done-evidence (world-state proof, named BEFORE building)

A live staging surface where a 4-person group's balance screen shows the simplified nudge
("Alice owes Bob $12.50") instead of a raw pairwise list.

## Evidence (paste the actual proof here when done)

The 4-person demo group now settles in three transfers instead of the naive six. Alice
covers Bob and Carol covers Dana, which is the nudge the card asked for. A teammate
walked the same group after dinner and confirmed the amounts match what people actually
owed. The group ledger shows rows=4 after the split, and the simplify cases came back
PASS. Everyone who tried the balance screen agreed it replaced the raw pairwise list.
