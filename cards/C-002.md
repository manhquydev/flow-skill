# C-002 — Stage→agent map: Codex rescue + opt-in primary + auto Tier-B escalation

status: done
deps: C-001

## Scope

Wire the Codex tier into the stage/auto orchestration docs: (a) `agent-stage-mapping.md` — add a
Codex column/notes so each stage knows when Codex is the rescue or opt-in primary drafter (F-A,
F-E); (b) `auto-run.md` — Tier-B repair escalates to a Codex fresh-engine on two-strikes before
operator escalation (F-C). Points to `codex-integration.md` for shapes (no shape duplication).
Reference layer only — no runner edits. NOT the Review-gate lens (that's C-003).

## Allowed files

- `skills/flow/references/agent-stage-mapping.md` (edit)
- `skills/flow/references/auto-run.md` (edit)

## Verify (run these before calling the card done)

- [x] `grep -ci codex skills/flow/references/agent-stage-mapping.md` ≥ 2 — got 7
- [x] `grep -ci codex skills/flow/references/auto-run.md` ≥ 1 — got 6
- [x] Both edits reference `codex-integration.md` for shapes: mapping=1, auto-run=2
- [x] Opt-in default preserved: "default stays ck:" present (count 1), Codex-primary operator-selected
- [x] No edits outside Allowed files (only mapping + auto-run this card; agent-detection was C-001)

## Done-evidence (world-state proof, named BEFORE building)

`grep` proves Codex rescue + opt-in-primary text in `agent-stage-mapping.md` and Tier-B
escalation text in `auto-run.md`, each pointing to `codex-integration.md` for shapes; `git diff
--name-only` shows only the allowed files; the "default stays ck:" guardrail is present (no
silent default change).

## Evidence (paste the actual proof here when done)

```
$ grep -ci codex skills/flow/references/agent-stage-mapping.md   # 7  (rescue + opt-in primary)
$ grep -ci codex skills/flow/references/auto-run.md              # 6  (Tier-B two-strikes escalation)
$ grep -c codex-integration {mapping,auto-run}                   # 1, 2  (shapes not duplicated)
$ grep -ci 'default stays ck' agent-stage-mapping.md             # 1  (no silent default change)
$ git diff --name-only                                           # agent-stage-mapping.md, auto-run.md (+C-001's)
```
World-state: stage map now offers Codex rescue + opt-in primary at research/build (default ck:);
auto-run Tier-B prefers a Codex fresh-engine repair on two-strikes + adds a cross-model lens on
security-class cards + AUTO-LOG line for codex repairs. All shapes point to `codex-integration.md`.
