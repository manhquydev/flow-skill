# DEBT - deliberate gate-skips (a loan, written down)

- [ ] DEBT: macOS-only: eval harness _run_with_timeout fallback does not bound a slow/stuck claude call -- billable eval verb could run an API call past --timeout on macOS (bash 3.2 stock /bin/sh), unbounded cost risk; ubuntu+windows unaffected (both have real timeout/gtimeout); 3 targeted fixes tried against real 3-OS CI (bash-3.2 empty-array _cleanup_tds crash - fixed a separate real bug; bare wait vs wait $PID; cat|claude pipe vs direct < file redirect) none resolved this specific one; mechanism still unconfirmed (kill -TERM $pid not reaching the child through sh -c, or wait not unblocking on a signal-killed child) -- diagnosis helper: scripts/macos-timeout-watchdog-diag.sh (PATH-masked; no gtimeout-on-PATH assumption); CI evidence links: none yet (operator checkpoint, <=2 macos-latest PR-to-master iterations) -- SHIPPED GUARD 2026-08-14: live artifact eval on darwin without timeout/gtimeout refuses by default (names unbounded-billing risk + FLOW_EVAL_UNBOUNDED=1); --report and replay skip the guard; the close-before condition "relying on --timeout as a hard cost cap on macOS" is now guarded by refuse-by-default -- entry stays open for mechanism diagnosis -- close before: before treating the fallback as a confirmed bound, or before removing the refuse-guard -- opened 2026-07-10
