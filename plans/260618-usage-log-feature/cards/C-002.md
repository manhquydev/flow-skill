# C-002 — Rollup JSONL→usage_event + `flow usage` stats

status: done
deps: C-001
implements: FR6, FR7

## Scope

Add the durable mirror + analytics on top of the JSONL produced by C-001:
- Migration `skills/flow/harness/schema/006-usage-event.sql` (I7): `usage_event` table (columns per contract) with `UNIQUE(src,line_no)`; `rollup_cursor(src PK, last_offset, updated_at)`; `INSERT INTO schema_version (version) VALUES (6);`.
- `flow_harness.py rollup` (I8): read each sink from `rollup_cursor.last_offset`, parse new JSONL lines, `INSERT OR IGNORE` into `usage_event` (idempotent), advance cursor; emit `{"rolled":N,"skipped":M}`. Skip malformed lines (R4 tolerant reader).
- `flow_harness.py usage` (I9): compute + print numeric cycle-time (per `cycle_id`: max ts − min ts), gate fail-rate (next|check: fail/total), kill rate, per-stage dwell (avg duration_s by stage_to), total count.
- Wire `usage)` into `flow.sh` dispatch (I10): rollup then usage, read-only, exit 0.

NOT in this card: the capture core (C-001); tests/docs/version (C-003).

## Allowed files

- `D:\project\flow\flow-skill\skills\flow\harness\schema\006-usage-event.sql` (new)
- `D:\project\flow\flow-skill\skills\flow\harness\flow_harness.py`
- `D:\project\flow\flow-skill\skills\flow\runner\flow.sh` (dispatch line + `cmd_usage` only — additive)

## Verify (run these before calling the card done)

- [x] After ≥1 real cycle's events exist: `python flow_harness.py rollup` → `rolled>0`; re-run → `rolled:0` (idempotent).
- [x] `sqlite3 .flow/harness.db 'select count(*),count(distinct src||line_no) from usage_event'` → two numbers equal (no dupes).
- [x] `bash flow.sh usage` prints ≥4 numeric metrics (cycle-time, gate fail-rate, kill rate, dwell); exit 0.
- [x] A deliberately malformed JSONL line is skipped, not fatal (rollup exits 0).

## Done-evidence (world-state proof, named BEFORE building)

`flow.sh usage` on this device prints real numeric build analytics computed from the C-001 JSONL, and a second `rollup` adds 0 rows — pasted command output + idempotency proof.

## Evidence (paste the actual proof here when done)

Verified against the SOURCE skill, scratch root on `/d/` (so Git Bash and Windows-python agree on the path). `bash -n` clean; `flow_harness.py` runs (proves valid Python). Migration 006 auto-applies via `_db.migrate` (schema_version → 6).

Generated 5 real events in one cycle (next pass, next gate-fail, check fail, card, status) + appended a malformed line, then:

**Rollup + idempotency + malformed-skip:**
```
$ flow_harness.py rollup          -> {"rolled": 5, "skipped": 1}    # malformed line skipped, not fatal
$ flow_harness.py rollup          -> {"rolled": 0, "skipped": 0}    # idempotent: nothing re-inserted
```
**No duplicates (UNIQUE(src,line_no)):** `rows=5 distinct=5 EQUAL=True`.

**`flow usage` — ≥4 numeric metrics from real data:**
```
flow usage - D:/project/flow/_c002scratch/.flow/events.jsonl
  events total:         5
  gate fail-rate:       67%  (2/3 next|check failed)
  cycles started:       1
  cycles reached cards: 1  (abandonment proxy: 0 not yet at cards)
  cycle-time (s):       min=9 median=9 max=9
  per-stage dwell (avg duration_s of 'next'):
    00-idea      n=1 avg=1s
  commands: next 2 | status 1 | check 1 | card 1
```
Note: "kill rate" was NOT mechanically capturable (no kill event exists), so it was honestly replaced with a **cycle-completion proxy** (cycles started vs reached-cards) rather than fabricated. Portability scan of the new `cmd_usage` region → 0 GNU-only constructs.

