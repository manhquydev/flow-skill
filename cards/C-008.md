# C-008 — Harden the doc-contract regression guard (clause-bound, not keyword-presence)

status: done
deps: C-006, C-007

## Scope

Fix the weakest dimension (tests scored 38): the guard asserted bare keywords (`never`, `installed`)
so an invariant could be deleted/inverted while staying green. Rewrite `test_flow_codex_integration.sh`
to bind each assertion to its actual clause, add the previously-unguarded surfaces (auto-run.md
Tier-B + AUTO-LOG + 2nd-strike gating, the `setup --json` probe command, the durable-metric hook,
the contract's USABLE two-state), an ANTI-PATTERN guard (`lacks` — auto-run must NOT call Codex on
first red), and tolerant alternations for wording-sensitive concepts. Also drop the MSYS-aborting
`grep -F`. Test file only.

## Allowed files

- `tests/test_flow_codex_integration.sh`

## Verify (run these before calling the card done)

- [x] `bash tests/test_flow_codex_integration.sh` exits 0 — 25 passed, 0 failed (was 19)
- [x] New coverage present: auto-run (3 assertions incl. anti-pattern `lacks`), `setup --json` probe, durable hook, contract USABLE
- [x] Anti-pattern guard works: `lacks` asserts auto-run has NO "prefer Codex on first red" (cost-gate)
- [x] No `grep -F` (MSYS abort); assertions are clause-bound (`hasE`) or literal (`has`, BRE)
- [x] No edits outside Allowed files

## Done-evidence (world-state proof, named BEFORE building)

`bash tests/test_flow_codex_integration.sh` → "RESULT: 25 passed, 0 failed"; the guard now binds to
clauses (e.g. `never auto-(pass|fail)`, `never (select|route) ... installed alone`) + a negative
`lacks` assertion, so deleting/inverting an invariant fails the suite. git diff = only the test file.

## Evidence (paste the actual proof here when done)

```
$ bash tests/test_flow_codex_integration.sh | tail -1
RESULT: 25 passed, 0 failed
# new clause-bound assertions incl: 'never auto-(pass|fail)', 'never route ... installed alone',
# 'setup --json' (probe), auto-run 'two-strikes deadlock', lacks 'prefer codex ... first red',
# durable 'intervention add', contract 'USABLE'. grep -F removed (MSYS SIGABRT).
$ git diff --name-only   # tests/test_flow_codex_integration.sh
```
World-state: the regression guard is now load-bearing — an edit that weakens detect-and-degrade,
the cost gate, gate parity, or re-introduces first-red Codex calls now FAILS the suite (D4-1..D4-6).
