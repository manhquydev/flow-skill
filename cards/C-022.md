# C-022 — v0.12.2 release: language-aware review

status: done
deps: C-021
group: release
implements: P-REL

## Scope

Cut v0.12.2 (patch — closes the last v0.12 backlog item: language-aware Review lens, C-021).
Bump every declared version 0.12.1 -> 0.12.2 (`skills/flow/SKILL.md`, `portable-manifest.json`,
`.claude-plugin/plugin.json`) + README status lines; add a `0.12.2` CHANGELOG entry (language-
specialist Review lens: typescript-reviewer/.ts·.js, python-reviewer/.py, layered with code-reviewer,
composes with the security lens, detect-first degrade, gate-parity preserved); update
`docs/quality-metrics.md` with the v0.12.2 run + test counts. `/flow coherence` must PASS. Doc/manifest only.

## Allowed files

- `skills/flow/SKILL.md` · `portable-manifest.json` · `.claude-plugin/plugin.json`
- `README.md` · `README_VN.md` · `CHANGELOG.md` · `docs/quality-metrics.md`

## Verify (run before calling the card done)

- [x] All declared versions = `0.12.2` (SKILL.md, portable-manifest.json, plugin.json)
- [x] `flow.sh coherence` → PASS
- [x] CHANGELOG has a `0.12.2` entry covering C-021
- [x] READMEs + quality-metrics updated (version + live test counts)
- [x] No edits outside Allowed files
- [x] `bash tests/run_all.sh` → ALL SUITES PASSED

## Done-evidence (world-state proof, named BEFORE building)

`flow.sh coherence` prints PASS at 0.12.2; CHANGELOG has the 0.12.2 entry; `bash tests/run_all.sh`
ends ALL SUITES PASSED with the count recorded in quality-metrics.

## Evidence (paste actual proof here when done)

```
flow.sh coherence -> PASS (0.12.2 in SKILL.md/plugin.json/manifest)
CHANGELOG 0.12.2: language lens (C-021) + honest note on the grep -oP -> sed -E macOS portability fix
README + quality-metrics -> live count 20 suites / 479 checks (corrected from a 482 estimate)
bash tests/run_all.sh -> ALL SUITES PASSED
```
Note: macOS-portability of the sed -E parse is verified by construct (BSD+GNU) + the project's
existing sed -E usage; the macos-latest CI job on push is the live proof.
