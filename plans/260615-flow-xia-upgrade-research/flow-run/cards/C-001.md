# C-001 — accessed_count read-only recall signal

status: done
deps: none
implements: FR2

## Scope

Add a usage signal to the durable harness: a new `accessed_count` column, incremented when
`recall`/`query` surfaces a row, with recall output ordered most-reused-first. **Read-only signal:
no prune, no delete; security-class rows hard-excluded from any deprioritization.** ONE thing:
the reuse-counting signal. (Repo root for all paths below: `D:\project\flow\flow-skill`.)

## Allowed files

- `skills/flow/harness/schema/005-accessed-count.sql` (new migration)
- `skills/flow/harness/flow_harness.py` (increment-on-read + reuse ordering + security-class guard in `cmd_query`)
- `skills/flow/harness/README.md` (document signal + no-prune rule + guard)
- `tests/test_flow_accessed_count.sh` (new dedicated suite), `tests/run_all.sh` (register)

(Contract amended first: accessed_count tables narrowed to {decision, trace, backlog} — the
recall-surfaced query reads; story matrix left as a status view. Test paths corrected to repo-root
`tests/` — the card's original `skills/flow/tests/` path was wrong.)

## Verify (run these before calling the card done)

- [x] Migration is idempotent: apply twice, `schema_version` bumps once, no error. (suite case A)
- [x] `recall` increments `accessed_count` for surfaced rows (query DB before/after shows +1). (case B/F)
- [x] Recall output orders most-reused-first. (case C)
- [x] A test asserts **zero rows are ever deleted** by this feature. (case D/F)
- [x] A test asserts a security-class row is NOT deprioritized. (case E/G)
- [x] Full `run_all.sh` green (≥17 suites), 0 regressions to existing recall tests. (ALL SUITES PASSED)

## Done-evidence (world-state proof, named BEFORE building)

Pasted terminal output showing: (a) the new/extended `test_flow_recall.sh` cases passing,
(b) a before/after `accessed_count` query proving the increment, (c) the `run_all.sh` summary line
showing all suites pass with 0 failures. (For this card "world-state" = the installed skill's
command behaving + the test rig green, per the skill done-definition.)

## Evidence (paste the actual proof here when done)

Real command behavior + ground-truth suite (the harness CLI executed for real, not "tests pass"):

```
$ bash tests/test_flow_accessed_count.sh
A) compile + migration 005 idempotent + accessed_count exposed   -> ok x4
B) accessed_count increments on each query read                  -> 0 then 2 after two reads
C) most-reused-first ordering                                     -> higher-reuse sorts first
D) reads NEVER delete a row (no-prune invariant)                 -> still 2 rows after 5 reads
E) security-class row sorts FIRST despite count 0                 -> ok
F) friction(trace) reads increment + not deleted                 -> ok
G) expanded security terms (JWT signing key rotation) sort first  -> ok
RESULT: 12 passed, 0 failed

$ bash tests/run_all.sh   ->  ALL SUITES PASSED  (exit 0; 17 suites, 0 regressions)
```

Cross-model adversarial review (Codex / GPT-5.x) found 2 real bugs the 17-suite pass missed:
(1) security regex too narrow (jwt/token/login) → FIXED by aligning to flow.sh's canonical
Tier-C pattern + JWT terms; (2) `_touch_accessed` UPDATE could fail a query on a read-only DB →
FIXED with best-effort try/except. Re-verified green after both fixes. Tier-C migration accepted
in DEBT.md.
