# C-003 — auto-derive session_id + PID-liveness lock reclaim

status: done
deps: none
implements: FR4

## Scope

Add `_session_id()` to skills/flow/runner/flow.sh: cascade
`FLOW_SESSION_ID` → `CLAUDE_CODE_SESSION_ID` → `CODEX_*`/`AGY_*` session vars →
`ppid:$PPID:$(hostname-short)`. Use it everywhere `FLOW_SESSION_ID` is currently read
(event logging + lock owner). Upgrade lock acquisition: reclaim a foreign lock when its PID
is dead (`kill -0`) OR TTL expired; otherwise hard-block. Keep existing lock file format.

## Allowed files

- `skills/flow/runner/flow.sh` (session id fn, lock acquire/owner, `_log_event` session field)
- `tests/` (regression test)

## Verify (run these before calling the card done)

- [x] In an agent session, any `flow.sh` command logs a non-empty `session_id` with NO manual export.
- [x] A lock held by a dead PID is reclaimed immediately (not after 900s TTL).
- [x] A lock held by a live foreign session still hard-blocks mutating commands.
- [x] New test passes; full suite green (existing concurrency-lock test must stay green).

## Done-evidence (world-state proof, named BEFORE building)

Paste event line(s) showing non-empty `session_id` without exporting it, plus terminal output
of the dead-PID reclaim path and the live-foreign-lock block, plus passing test name.

## Evidence (paste the actual proof here when done)

```
1) auto session_id (no FLOW_SESSION_ID export): "session_id":"d804ab80-ef4a-4bfb-8799-1bd27f920e0c"
2) dead-PID reclaim: NOTE: reclaiming a flow lock from a dead session [sid:DEADSESS] (pid 999999 no longer alive). -> PASS unlock
3) live foreign lock: BLOCKED: another flow session is active on this project ...
```
Implementation: `_session_env_id` cascade (FLOW_SESSION_ID → CLAUDE_CODE_SESSION_ID →
CODEX_*/AGY_* → tty → ppid) feeds both `_session_id` (event field) and `flow_lock_owner`
(strong id); `lock_acquire` adds same-host `kill -0` dead-PID reclaim before the TTL path.
Tests `tests/test_flow_concurrency_lock.sh` §L,§M. Suite: concurrency **30/0**, usage_log **32/0**.
