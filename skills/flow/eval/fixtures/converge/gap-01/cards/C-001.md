# C-001 - task create + list
status: done
implements: FR1

## Scope
POST /tasks and GET /tasks.

## Independent test
A user creates a task at /tasks and sees it returned by the list.

## Allowed files
src/app.py

## Verify (run these before calling the card done)
- [x] POST /tasks returns 201

## Done-evidence (world-state proof)
the deployed service returns the created task

## Evidence (paste the actual proof here when done)
verified against the running service
