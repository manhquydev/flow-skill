# C-001 — Codex detection + 4th "second-engine" tier in the agent ladder

status: done
deps: none

## Scope

Add Codex as a detectable 4th tier in `references/agent-detection.md` (ck:→bmad→built-in→**codex**)
and create one consolidated `references/codex-integration.md` that holds the Codex seam (detection
rule I1, cost gate, status protocol, the invocation surfaces from `flow/05-contract.md`). This is
F-A's detection half + F-D (detect-and-degrade + cost discipline). NOT the stage-mapping/review/auto
wiring (those are C-002/C-003). Documentation/reference layer only — no runner edits.

## Allowed files

- `skills/flow/references/agent-detection.md` (edit)
- `skills/flow/references/codex-integration.md` (new)

## Verify (run these before calling the card done)

- [x] `grep -c 'odex' skills/flow/references/agent-detection.md` ≥ 3 (tier documented) — got 20
- [x] `codex-integration.md` exists and contains the detection rule, the degrade rule, and the cost gate
- [x] Live detection proof: the host shows `codex:codex-rescue` available AND the plugin dir exists → tier ELIGIBLE; doc states exact degrade behavior when absent
- [x] No edits outside Allowed files (`git diff --name-only` = only the 2 files)

## Done-evidence (world-state proof, named BEFORE building)

`git diff --stat` shows only the 2 allowed files; `grep` proves the codex tier text is present in
the ladder; a real command run proves detection resolves to ELIGIBLE in this host
(`ls ~/.claude/plugins/cache/openai-codex` returns the plugin) and the doc states the exact
fallback when the registry lacks `codex:codex-rescue`.

## Evidence (paste the actual proof here when done)

```
$ grep -ci codex skills/flow/references/agent-detection.md
20                                  # tier documented (>=3)
$ grep -ciE 'detection|degrade|cost gate' skills/flow/references/codex-integration.md
8                                   # detection + degrade + cost-gate present
$ ls -d ~/.claude/plugins/cache/openai-codex && echo ELIGIBLE
ELIGIBLE: openai-codex plugin dir present   # I1 resolves ELIGIBLE in this host
$ git diff --name-only ; git status --short | grep codex-integration
skills/flow/references/agent-detection.md   # only allowed file edited
?? skills/flow/references/codex-integration.md  # only allowed new file
```
Degrade behavior documented in `codex-integration.md` §Detection: absent registry+plugin →
silent fall-back to ck:→bmad→built-in with announce. World-state: the reference layer now
carries the Codex tier + detection/degrade/cost-gate; detection verified live ELIGIBLE.
