# C-002 — flow.sh: recall usage block + usage --prune dispatch + gate-fail-reason capture

status: done
deps: C-001
implements: FR1 (I2), FR3 (I4 dispatch), FR4 (I5 shell)

## Scope

The runner layer in `skills/flow/runner/flow.sh` (consumes C-001's harness surfaces):
- **I2 recall hook (FR1):** in `cmd_recall`, after the existing read-backs, append the output of `flow_harness.py usage --summary` under a `USAGE (mechanical log):` line — best-effort via `harness_available`/`_python`; print nothing extra if empty or unavailable (recall never breaks).
- **I4 dispatch (FR3):** `flow.sh usage --prune [--keep N] [--global]` → route to `flow_harness.py prune ...` (plain `flow.sh usage` unchanged). Read the first arg of `usage`.
- **I5 shell (FR4):** in the gate-fail path of `cmd_next`/`cmd_check`, set `FLOW_LAST_GATE_FAIL="fill:<N>,unchecked:<M>"` (counts already computed by the gate scan) before the failing `return 1`; add `gate_fail_reason` to the event JSON in `_log_event` (reads `${FLOW_LAST_GATE_FAIL:-}`), default empty. Must NOT change any gate exit code or control flow.

NOT here: harness python (done in C-001); tests/docs/version (→ C-003).

## Allowed files

- `D:\project\flow\flow-skill\skills\flow\runner\flow.sh`

## Verify (run these before calling the card done)

- [x] In a scratch project with events: `bash flow.sh recall` shows a `USAGE (mechanical log):` line with real figures; with `FLOW_HARNESS_DISABLE=1` → recall output identical minus that line (no error, exit 0).
- [x] `bash flow.sh usage --prune --keep 3` caps the project sink to 3 lines and prints the dropped count; `bash flow.sh usage` (no flag) still prints stats unchanged.
- [x] A failing `bash flow.sh next` (stage with FILL) → the recorded event has non-empty `gate_fail_reason` (e.g. `fill:1`); a passing `next` → empty; **both exit codes identical to pre-change** (0 on pass, 1 on fail).
- [x] Portability: scan added lines for GNU-only constructs (`\b`/`grep -P`/`%N`/`sed -i`) → none. `bash -n flow.sh` clean.

## Done-evidence (world-state proof, named BEFORE building)

On this device: `flow.sh recall` shows the live usage block (and cleanly omits it when disabled), `flow.sh usage --prune` caps a real sink, and a real failed gate records its `gate_fail_reason` with the gate exit code unchanged — pasted real outputs.

## Evidence (paste the actual proof here when done)

Verified on the SOURCE runner, `/d/` scratch, HOME isolated. `bash -n` clean. Generated real data: greenfield `next` (unlock 00, exit 0) + 2nd `next` (gate fail at 00-idea, exit 1).

**FR4 gate-fail reason + stage attribution** (a fix surfaced by live verification: the fail-path now also sets `FLOW_LOG_STAGE_TO="$cur"` so the event attributes its stage — without it `top-fail-stage` was `none` and propose got no stage):
```
fail event: stage_to='00-idea' reason='fill:4,unchecked:3'   # passing next records reason=''
```
Exit codes unchanged: pass `next`=0, fail `next`=1.

**FR1 recall USAGE block** (rolls up then one-line digest):
```
USAGE (mechanical log): cycles=1 reached-cards=0 | cycle-time s min/med/max=11/11/11 | gate fail-rate=50% | top-fail-stage=00-idea(1/2)
```
No-fail: `FLOW_HARNESS_DISABLE=1 ... recall` → exit 0, **no** USAGE line (degrades silently).

**FR3 `flow usage --prune --keep 1`** capped the sink: `{"kept":1,"dropped":3}`; plain `flow usage` still prints stats. (Line counts include each command's own logged event — expected.)

**Portability:** new `cmd_recall`/`cmd_usage` lines scanned for `grep -P`/`%N`/`sed -i` → none. `bash -n` OK.

