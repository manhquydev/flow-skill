# C-009 — P3 Contract-gate self-consistency lens + data-boundary note

status: done
deps: C-006, C-007

## Scope

Two review-driven additions: (1) **P3 / D5-1 — close the gate false-pass at its source.** The
adversarial review confirmed the Contract gate passed an internally-inconsistent seam (the I1
drift), caught only later by the card-Review cross-model lens. Add a Contract-stage challenge in
`gate-rules.md`: run a same-model self-consistency pass over the contract vs the seam docs, and —
when the codex tier is USABLE — an OPTIONAL opt-in cross-model check, because the contract is the
one artifact every downstream card trusts as ground truth. (2) **D3-F1 — data boundary.** Add a
"Data boundary" note beside the Auth clause in `codex-integration.md`: selecting Codex sends the
ScopedBrief (diff + contract + PRD + law) to OpenAI's API. Reference layer only.

## Allowed files

- `skills/flow/references/gate-rules.md`
- `skills/flow/references/codex-integration.md`

## Verify (run these before calling the card done)

- [x] `gate-rules.md` Stage-05 has self-consistency + opt-in cross-model challenge (count 3)
- [x] Ties to "ground truth every downstream card trusts" rationale (count 1)
- [x] `codex-integration.md` has the "Data boundary" note (ScopedBrief → OpenAI, operator data terms) (count 1)
- [x] No edits outside Allowed files (gate-rules.md, codex-integration.md)

## Done-evidence (world-state proof, named BEFORE building)

`grep` shows the new Contract-stage self-consistency/cross-model challenge in gate-rules.md and the
Data-boundary note in codex-integration.md; `git diff --name-only` = only the 2 allowed files. The
gate that birthed the false-pass now has a challenge that would have caught the I1 drift.

## Evidence (paste the actual proof here when done)

```
$ grep -ci 'self-consistency\|cross-model' gate-rules.md   # 3  (Contract-stage challenge added)
$ grep -ci 'downstream card' gate-rules.md                 # 1  (ground-truth rationale)
$ grep -ci 'Data boundary' codex-integration.md            # 1  (ScopedBrief -> OpenAI note)
$ git diff --name-only   # gate-rules.md, codex-integration.md
```
World-state: the Contract gate — where this run's false-pass was born — now carries a
self-consistency challenge (+ opt-in cross-model check when USABLE) that would have caught the I1
drift at its source; the seam documents the data-egress boundary, not just credential handling.
